const roles = {
  builder:   require("roles.builder"),
  harvester: require("roles.harvester"),
  upgrader:  require("roles.upgrader"),
  tower:     require("roles.tower"),
};

module.exports = roles;